import styled from 'styled-components';

import CountUp from 'react-countup';

const Counter = styled(CountUp)`
  font-size: 72px;
  line-height: 1.2;
  font-weight: 400;
  color: #202124;
`

export { Counter };